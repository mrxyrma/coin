/**
 * Гнездо под монету в футляре — в экранных координатах.
 *
 * Углубление ложемента живёт в 3D, внутри повёрнутого футляра, а якорь
 * монеты — плоский DOM-блок. Совпасть они не могут в принципе: наклон
 * футляра сдвигает углубление и по экрану, и по глубине.
 *
 * Поэтому футляр каждый кадр проецирует центр своего углубления обратно
 * в CSS-пиксели и кладёт сюда, а монета на подлёте целится в эту точку
 * вместо своего якоря.
 */
export const caseSlot = {
  ready: false,
  /** Центр углубления в координатах вьюпорта, CSS px */
  x: 0,
  y: 0,
  /** Видимый диаметр углубления, CSS px */
  size: 0,
}
