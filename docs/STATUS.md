# Где мы остановились

Дата: 6 августа 2026. Полный план — [PLAN.md](./PLAN.md).

## Сделано (этапы 1–3, 6, 7)

| Этап | Состояние |
|---|---|
| 1. Каркас и вёрстка | ✅ 8 секций, десктоп + мобайл, типографика и цвета из макета |
| 2. Скролл-движок | ✅ Lenis + ScrollTrigger + система вейпоинтов, отладочный режим |
| 3. Canvas и болванка | ✅ pixel-камера, цилиндр с процедурными текстурами аверс/реверс |
| 4. Продакшн-модель | 🟡 конвейер готов, модель на сайте; **нет двусторонней монеты и серебра** |
| 5. Футляры и укладка | ⛔ ждёт ассеты от дизайнера |
| 6. Интерактив | ✅ табы Золото/Серебро, переворот монеты по клику и с клавиатуры |
| 7. Перф и фолбэки | ✅ рендер по изменению, пауза вне вкладки, reduced-motion, no-WebGL |

## Как запустить

```bash
npm install
npm run dev          # http://localhost:5173
npm test             # 9 тестов на математику траектории
npm run build        # tsc + vite build
npx eslint .
```

Отладка траектории: **http://localhost:5173/?coin=debug** — DOM-кружок летит по той же
кривой, что и 3D-монета, с выводом координат, диаметра и угла.

## Деплой на GitHub Pages

`.github/workflows/deploy.yml` собирает и выкладывает сайт на каждый push в `main`.
Секреты не нужны — токен выдаётся самому workflow.

Разовая настройка в репозитории: **Settings → Pages → Source: GitHub Actions**.

Сайт GitHub Pages живёт в подкаталоге `/<имя-репозитория>/`, поэтому все ссылки на
ассеты должны быть с префиксом. Имя репозитория нигде не зашито: workflow передаёт его
через `BASE_PATH`, `vite.config.ts` кладёт в `base`, а код берёт `import.meta.env.BASE_URL`
(`src/three/CoinModel.tsx`, `src/three/Case.tsx`, `src/ui/Menu.tsx`). **Новые пути к файлам
в `public/` писать так же — абсолютный «/…» на Pages отвалится в 404.**

Проверить сборку локально ровно в том виде, в каком её увидит Pages:

```bash
BASE_PATH=/coin/ npm run build && BASE_PATH=/coin/ npm run preview
# http://localhost:4173/coin/
```

## Модели от дизайнера

Исходники (`public/models/moneta_*.glb`, по ~95 МБ) **в репозиторий не коммитятся** —
они в `.gitignore`. Хранятся локально; после переустановки попросить у дизайнера заново.

Прогон через конвейер — 94 МБ → 1.7 МБ на сторону:

```bash
node scripts/optimize-coin.mjs public/models/moneta_aist.glb   public/models/coin-obverse.glb
node scripts/optimize-coin.mjs public/models/moneta_zdanie.glb public/models/coin-reverse.glb
```

Контракт результата закреплён тестами в `src/three/coinModel.test.ts`: центр в нуле,
диаметр 1, толщина по Z, WebP ≤ 2048, 10–80 тыс. треугольников.

**Открыто:** дизайнер прислал две односторонние модели (у каждой изнанка — пустой диск),
поэтому при повороте монета показывает болванку. Нужна одна двусторонняя модель.
Серебра тоже нет: обе модели золотые, серебро сейчас получается обесцвечиванием
карты цвета в `src/three/CoinModel.tsx` — временная мера.

## Ключевые файлы

| Файл | Зачем |
|---|---|
| `src/lib/trajectory.ts` | **Единственное место с цифрами анимации**: наклон, полуобороты, вынос дуги `bulgeOut` |
| `src/lib/coinPath.ts` | Обмер якорей и чистая функция `samplePath` — вся математика пути |
| `src/lib/pixelCamera.ts` | Камера, в которой 1 мировая единица = 1 CSS-пиксель |
| `src/lib/coinStore.ts` | Металл и сторона монеты; доворот от клика живёт вне React |
| `src/ui/CoinAnchor.tsx` | Невидимый якорь — позицию монеты задаёт CSS секции, не JS |

## Что делать дальше

1. **Запросить ассеты у дизайнера** — [designer-request.md](./designer-request.md),
   документ готов к пересылке как есть. Техническая версия того же — в
   [PLAN.md](./PLAN.md), раздел «ТЗ дизайнеру на ассеты». Модель монеты самая
   долгая по срокам, просить в первую очередь.
2. Когда приедут: этап 4 (модель, HDRI, DRACO+KTX2), затем этап 5 (футляры,
   сцена укладки в ложемент).
3. Не блокировано ассетами: тонкая подгонка выносок в «уникальности»,
   hover-реакция монеты на курсор, прогон вёрстки по ширинам 768/1024/1920.

## Открытые вопросы

- Текста реверса в макете нет — в `src/sections/Appearance.tsx` стоит заглушка.
- Логотип Сбера — лицензионный ассет, в `src/ui/SberLogo.tsx` серый плейсхолдер.
- Шрифты SB Sans есть у заказчика; до подключения работают фолбэки
  (блок `@font-face` заготовлен в `src/index.css`).
- Макет прототипный: при обновлении правятся CSS-позиции `.coin-anchor`
  и числа `bulgeOut` — код траектории не трогается.
